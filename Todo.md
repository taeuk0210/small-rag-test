### 전체 파이프라인

1. `user_input`, `retrieved_contexts`, `reference`(ground_truth) 데이터셋 생성
     - 도메인은 업무 보조, 법령 검색을 예시로 생성
     - 이전 대화 맥락(`history`) 싱글턴/멀티턴 대화를 구분 -> 멀티턴의 경우 문맥유지력을 관찰해야하니까 대화 내용 설계를 잘 해야 함
     - `retrieved_contexts`가 요약된 경우/아닌 경우 구분
     - `retrieved_contexts`가 수를 비교할 수 있도록 구분(Top 2 ~ 7 예상)
     - `retrieved_contexts`에 답변과 관련 없는 노이즈 작성
     - `retrieved_contexts`에서 순위를 조정해보기(안정성 측정을 위해)
     - `reference`가 현재는 알 수 없다는 답변인 것도 있어야 함

2. LLM 별로 `response` 저장
3. `user_input`, `retrieved_contexts`, `reference`, `response` 활용해서 지표 측정
4. 모델별 지표 비교 및 한계점/특징/인사이트 도출(가능하면)


### 프롬프트 작성
- vLLM 으로 보낼 LLM 프롬프트를 설계 후 모델별 돌려쓸지 말지 등등 선택하기

### 샘플 데이터 작성

1. **정보 추출 및 요약 (Retrieval-to-Answer)**  
     - 요약 유무: 요약된 컨텍스트를 주었을 때 할루시네이션 감소율을 측정(sLLM은 정보 밀도가 높을수록 추론 성공률이 올라가는 경향이 있음)
     - 청크 수(Top 3~6): '정답률 대비 토큰 비용' 효율성을 계산해 보세요. Top 6에서 정확도가 5% 오르는데 비용이 2배 든다면, sRAG에서는 Top 3가 최적이라는 결론을 낼 수 있음
     - 노이즈 삽입: 관련 없는 문장이 섞였을 때 정답을 골라내는 **노이즈 내성(Noise Robustness)**을 확인

2. **대화 문맥 유지 (Conversation Memory)**
    - 대화 턴(3~7턴): 단순히 답변 여부만 보지 말고, **지시사항 유지력**을 확인(예: 5턴이 넘어가면 초기 지시를 어기기 시작하는지 확인) -> 핵심 지표: 턴이 깊어질수록 **답변의 일관성(Consistency)** 이 얼마나 무너지는지 기록

3. **추론 제어 및 안정성 (Constraint & Safety)**
     - 형식 강제: JSON 파싱 에러율을 측정(sLLM은 ```json 태그를 빼먹거나 괄호를 안 닫는 경우가 잦음)
     - 순서 영향: 정답이 담긴 청크를 1번에 두었을 때 vs 마지막에 두었을 때의 정답률 편차 확인(편차가 클수록 Reranker 도입의 근거가 됨)
     - 할루시네이션 방어: **모름**이라고 답변해야 할 상황에서 지어내는 비율을 측정

### 테스트 코드 작성
- main.py 를 기준으로 시작할텐데 적당히 가독성있게 모듈별로 분리해서 설계(HACCP 때 쓸 수 있도록)